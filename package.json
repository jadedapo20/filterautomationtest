{
  "name": "filterautomationtest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "pretest": "tsc --incremental -p ./tsconfig.json",
  "test": "playwright test -c tests-out",
  "test:chromium": "cross-env ENV=dev BROWSER=chromium npx cucumber-js --require features/steps/**/*.js",
  "test:firefox": "cross-env ENV=dev BROWSER=firefox npx cucumber-js --require features/steps/**/*.js",
  "test:webkit": "cross-env ENV=dev BROWSER=webkit npx cucumber-js --require features/steps/**/*.js",
  "test:all": "npm-run-all --parallel test:chromium test:firefox test:webkit"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@faker-js/faker": "^10.1.0",
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "faker": "^5.5.3",
    "playwright": "^1.56.1"
  }
}
